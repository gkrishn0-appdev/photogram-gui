<h1> <%= @the_user.username %> </h1>

<dl>
  <dt>ID</dt>
  <dd>42 </dd>
  <dt>Edit user</dt>
  <dd>Form goes here</dd>
</dl>

<h2>Own photos (<%= @the_user.own_photos.size %>) </h2>

<table border ="1">
  <tr>
    <th>Image</th>
    <th>Owner</th>
    <th>Caption</th>
    <th>Posted</th>
    <th></th>
  </tr>
<% @the_user.own_photos.each do |a_photo| %>
    <tr>
    <th> <img src = "<%= a_photo.image %>">
    </th>
    <th><%#= User.where({:id => a_photo.owner_id}).at(0).username %>
    <%= a_photo.poster.username %></th>
    <th><%= a_photo.caption %></th>
    <th><%= a_photo.created_at %></th>
    <th>Show details</th>
  </tr>
  <% end %>
</table>

